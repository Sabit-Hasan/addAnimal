/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'], // Add allowed image domains here
    },
    // Add other Next.js configurations if needed
};

export default nextConfig;
